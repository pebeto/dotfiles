# This dir symlinks into the repo. Track only the source files; ignore anything
# Emacs generates (most state goes to ~/.cache and ~/.local/share; this is a
# backstop).
*
!.gitignore
!early-init.el
!init.el
