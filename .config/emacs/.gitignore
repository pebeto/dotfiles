# This dir is symlinked into the repo. Only the two source files are tracked;
# anything Emacs generates here is ignored (most state is redirected to
# ~/.cache and ~/.local/share by early-init.el / init.el, this is a backstop).
*
!.gitignore
!early-init.el
!init.el
