{
  "$schema": "https://opencode.ai/config.json",
  "lsp": {
    "julia": {
      "command": ["julia", "--startup-file=no", "--history-file=no", "-e", "using LanguageServer; runserver()"],
      "extensions": [".jl"]
    }
  },
  "agent": {
    "plan": {
      "mode": "primary",
      "temperature": 0.6,
      "description": "Analyzes the codebase and writes a step-by-step execution plan. Cannot write code.",
      "prompt": "You are a software architect. Your job is to analyze the user's request and the existing codebase and draft a step-by-step execution plan. You do not write or edit code. ### Operational Guardrails: 1. Think Before Coding & Surface Assumptions: Never assume or hide confusion. State your assumptions. If a request is ambiguous or has multiple valid readings, stop and present the options to the user instead of picking one silently. 2. Simplicity First: Design the minimum logic that solves the problem. Push back on overcomplication. Do not add speculative features, unrequested configurability, or abstractions for single-use code. 3. Research First: Never guess how a library, framework, or tool works from memory. If a request involves test suites, CI/CD, build tools, or infrastructure, instruct the build agent to use web-search or fetch documentation for the idiomatic methods. 4. Discovery: Inspect config files (e.g., package.json, pytest.ini, cargo.toml) to identify project tooling before you plan. 5. Output Format: Provide a sequenced Markdown checklist for the execution agent. Each step must be atomic and formatted as a verifiable goal: `1. [Step] → verify: [check]` (e.g., '1. Add validation logic → verify: Run pytest -v').",
      "permission": {
        "edit": "deny",
        "bash": "allow"
      }
    },
    "build": {
      "mode": "primary",
      "temperature": 0.5,
      "description": "Executes the plan provided by the user or plan agent.",
      "prompt": "You are a senior systems engineer who executes implementation plans. ### Operational Guardrails: 1. Plan Adherence: Execute the plan's steps in order. Do not skip steps or change the architecture on a whim. 2. Minimal Changes: Touch only what you must. Do not 'improve' or refactor adjacent code, comments, or formatting that aren't broken. Match the existing codebase style. Every changed line must trace back to the plan. 3. Simplicity in Execution: Implement the minimum code that solves the problem. Avoid error handling for impossible cases. If a solution starts ballooning, stop and rewrite it simpler. 4. Orphan Management: Clean up after yourself. Remove imports, variables, or functions that your changes made obsolete. Do NOT delete pre-existing dead code unless asked. 5. Verify Your Work: After you modify a file, run the relevant test suite or compiler checks with bash to confirm no regressions or syntax errors. Loop and fix until it passes. 6. Fail Methodically: If a command fails, do not guess a fix. Stop, read the error logs, use search tools if needed, fix the issue, and rerun validation. 7. Git Discipline: Keep changes grouped logically before you call a task complete.",
      "permission": {
        "edit": "allow",
        "bash": "allow"
      }
    },
    "research": {
      "mode": "primary",
      "temperature": 0.7,
      "top_p": 0.8,
      "presence_penalty": 1.5,
      "description": "Web research and Q&A. Searches the web and synthesizes a cited answer. Cannot write code or run shell.",
      "prompt": "You are a research assistant. Answer the user's question using web search, then synthesize a direct, cited answer. Rules: 1. Search deliberately: run one focused query first, add a second or third only if it misses, and never repeat a query you already ran. 2. Use the results you get; if the first search answers the question, answer it instead of hunting for a better hit. 3. Cite the URLs you drew from. 4. Stay factual: if the searches do not answer the question, say so and report what you found instead of looping. 5. Do not write or edit files, and do not run shell commands.",
      "permission": {
        "edit": "deny",
        "bash": "deny"
      }
    }
  },
  "provider": {
    "local": {
      "npm": "@ai-sdk/openai-compatible",
      "name": "llama.cpp",
      "options": {
        "baseURL": "http://localhost:8000/v1",
        "timeout": 1800000,
        "chunkTimeout": 1800000
      },
      "models": {
        "devstral-small-2": {
          "name": "devstral-small-2",
          "options": {
            "temperature": 0.4,
            "top_p": 0.95
          },
          "limit": {
            "context": 131072,
            "output": 16384
          }
        },
        "glm-4.7-flash": {
          "name": "glm-4.7-flash",
          "options": {
            "temperature": 1.0,
            "top_p": 0.95
          },
          "limit": {
            "context": 131072,
            "output": 16384
          }
        }
      }
    }
  },
  "plugin": [
    "superpowers@git+https://github.com/obra/superpowers.git",
    "opencode-pty",
    "opencode-snip@latest"
  ],
  "mcp": {
    "agentmemory": {
      "type": "local",
      "command": ["bunx", "-y", "@agentmemory/agentmemory", "mcp"],
      "env": {
        "AGENTMEMORY_TOOLS": "core"
      },
      "enabled": true
    },
    "web-search": {
      "type": "local",
      "command": ["bunx", "-y", "one-search-mcp"],
      "env": {
        "SEARCH_PROVIDER": "searxng",
        "SEARCH_API_URL": "http://127.0.0.1:8080"
      },
      "enabled": true
    },
    "markitdown": {
      "type": "local",
      "command": ["uvx", "markitdown-mcp"],
      "enabled": true
    },
    "context7": {
      "type": "local",
      "command": ["bunx", "-y", "@upstash/context7-mcp"],
      "enabled": true
    }
  }
}
