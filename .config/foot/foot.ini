[main]
# Font size lives in the per-host file, since it depends on whether that machine's output
# runs scaled. install.sh links ~/.config/foot-host.ini to hosts/<name>.ini.
include=~/.config/foot-host.ini

pad=0x0

initial-window-mode=windowed

[csd]
size=0

[key-bindings]
fullscreen=F11
