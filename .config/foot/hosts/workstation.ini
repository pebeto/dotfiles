# 4K panel run unscaled, so the font carries the size the compositor would otherwise scale.
# Changing this changes the clock popup's pixel width: keep $cal_popup_w in
# sway/hosts/workstation.conf in step (52 columns plus the 3 px border twice).
font=JuliaMono:size=16.5
